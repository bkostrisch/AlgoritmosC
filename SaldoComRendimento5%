#include <stdio.h>
#include <stdlib.h>

int main() {
	float saldo,total;
	printf("Digite o saldo:\n");
	scanf("%f", &saldo);
	total = saldo+(saldo*5/100);
	system("cls");
	printf("O saldo mais o rendimento em 5%% %c: %f", 130, total);
	return(0);
}
