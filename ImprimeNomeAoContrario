#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
	char nome[50];
	int tamanho, i=0;
	printf("Digite um nome:\n");
	scanf("%[^\n]s", nome);
	tamanho = strlen(nome);
	system("cls");
	for(i=tamanho-1;i>=0;i--){
		printf("%c", nome[i]);
	}
	
	return 0;
}
